# To simulate the stack use .append and .pop

stack = [3, 4, 5]
stack.append(6)
stack.append(7)

stack

stack.pop()
stack

n = stack.pop()
n

stack.pop()
stack.pop()
stack.pop()

# If we call again the .pop() method it will generate an error because the list
# as not elements
